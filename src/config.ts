// Single source of truth for brand/site strings.
// Product name is set; domain is still TBD.
export const BRAND = "AI-Native Dev";

export const SITE = {
  // Public site URL (placeholder until domain is chosen).
  website: "https://example.com/",
  // Google Calendar booking link (shared with petrenko.cv).
  bookingUrl: "https://calendar.app.google/TyVxPuo9DK4J5PdC8",
  // Waitlist target — replace with the real waitlist URL/form when live.
  waitlistUrl: "#waitlist",
  // Underlying open-source platform.
  jeanUrl: "https://jean.build",
  tagline: "AI agents that ship production code — and prove it works.",
  subtitle:
    "An opinionated, self-hostable AI dev pipeline. Every change is test-driven, visually verified, and gated on acceptance criteria before it merges. Built on Jean, with a memory layer your agents actually keep.",
};
